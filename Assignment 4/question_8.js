function selectionSort(arr, size) {
    if (size == 0) {
        return
    }
    for (let i = 0; i < size - 1; i++) {
        let maxid=0
        if(arr[maxid]<arr[i]){
            maxid=i
        }
    }
}